If you are on a Mac, you can install the metal backend by running the following commands:

```bash
git clone https://github.com/geohot/tinygrad.git
cd tinygrad
python3 -m pip install -e ".[metal]"
```
